{
  "name": "print-server",
  "version": "1.0.0",
  "description": "Servidor local de impresión para taller de fotografía escolar",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "generate-password": "node scripts/generate-password.js",
    "build:css": "tailwindcss --content \"./public/**/*.html\" -o public/css/tailwind.min.css --minify"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "better-sqlite3": "^8.1.0",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "multer": "^2.0.0"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
